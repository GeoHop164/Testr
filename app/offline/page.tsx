export default function OfflinePage() {
  return (
    <main className="app-shell">
      <section className="frame">
        <header className="title-card">
          <span>QA SWIPE CONSOLE</span>
        </header>
        <section className="panel start-panel">
          <h1>Offline Mode</h1>
          <p>
            You appear to be offline. Reconnect and refresh to sync the latest app state.
          </p>
        </section>
      </section>
    </main>
  );
}
